aloha


In this assignment, I learned how to use Git and GitHub, including creating branches, committing changes, pushing to a remote repository, switching between branches and commits, and merging branches.

I also learned how to use a pretrained ResNet model to run inference on the MNIST dataset.

I also learned the importance of using .gitignore to avoid committing datasets, model weights, and other unnecessary files.